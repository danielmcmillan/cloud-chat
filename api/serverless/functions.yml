onConnect:
  handler: src/handler/connection/onConnect.handler
  events:
    - websocket:
        route: $connect

onDefault:
  handler: src/handler/message/default.handler
  events:
    - websocket:
        route: $default
